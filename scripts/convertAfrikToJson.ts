#!/usr/bin/env tsx
/**
 * AFRIK TXT → JSON Conversion Script (ONE-TIME — ARCHIVED)
 *
 * This script was used to convert all AFRIK source TXT files to JSON.
 * It has already been run successfully (1005/1005 files converted).
 * The TXT parsers it depended on have since been deleted.
 *
 * DO NOT RUN — kept for historical reference only.
 * The JSON source files are the authoritative source format going forward.
 */

// NOTE: This file is intentionally left as an archive.
// The parsers it imported no longer exist. Do not attempt to run this script.
// See: dataset/source/afrik/**/*.json for the converted output.

export {};
