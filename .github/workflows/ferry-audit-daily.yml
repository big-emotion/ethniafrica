# Managed by ferry-init. Re-run `npx -p @big-emotion/ferry ferry-init` to update.
# This workflow runs daily governance checks (spend monitoring, event pruning).

name: Ferry — Audit Daily

on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch: {}

concurrency:
  group: ferry-audit-daily
  cancel-in-progress: true

jobs:
  audit-daily:
    uses: big-emotion/ferry/.github/workflows/audit-daily.yml@v0.3.0
    secrets: inherit
